---
slug: "python-02-files-and-data"
title: "File Handling & Data Processing"
track: "Intermediate Python"
trackSlug: "intermediate-python"
week: 2
level: "Intermediate"
duration: 75
sourceUrl: "https://docs.google.com/presentation/d/1Na2qrUMFi3XpOOXx0rachPoL_97SJHR8/edit"
summary: "Read and write files safely, choose CSV for flat tables and JSON for structured data, and use context managers and libraries to avoid data loss."
summarySw: "Soma na uandike faili kwa usalama, chagua CSV kwa jedwali rahisi na JSON kwa data yenye muundo, na tumia context managers na maktaba kuzuia upotevu wa data."
summaryFr: "Lisez et écrivez des fichiers en sécurité, choisissez CSV pour les tableaux plats et JSON pour les données structurées, et utilisez les gestionnaires de contexte pour éviter les pertes."
activity: "Load a small CSV of community resources, calculate a count by category, and save a JSON summary. Handle a missing file without crashing the program."
objectives: ["Explain File mode in your own words.","Apply CSV to a realistic classroom or community example.","Complete the practice task and reflect on one improvement."]
keyIdeas: [{"term":"File mode","definition":"The instruction passed to open—such as read, write, or append—that determines what operations are allowed and whether existing content is preserved."},{"term":"CSV","definition":"A plain-text format for flat rows and columns that is easy to exchange with spreadsheets but stores values as text."},{"term":"JSON","definition":"A text format that preserves nested objects, arrays, numbers, booleans, and text for structured data exchange."}]
flashcards: [{"front":"File mode","back":"The instruction passed to open—such as read, write, or append—that determines what operations are allowed and whether existing content is preserved."},{"front":"CSV","back":"A plain-text format for flat rows and columns that is easy to exchange with spreadsheets but stores values as text."},{"front":"JSON","back":"A text format that preserves nested objects, arrays, numbers, booleans, and text for structured data exchange."}]
quiz: [{"question":"Which explanation best describes File mode?","choices":["The instruction passed to open—such as read, write, or append—that determines what operations are allowed and whether existing content is preserved.","A plain-text format for flat rows and columns that is easy to exchange with spreadsheets but stores values as text.","A text format that preserves nested objects, arrays, numbers, booleans, and text for structured data exchange.","A decorative feature with no effect on the task"],"answer":0,"explanation":"The instruction passed to open—such as read, write, or append—that determines what operations are allowed and whether existing content is preserved."},{"question":"Which explanation best describes CSV?","choices":["The instruction passed to open—such as read, write, or append—that determines what operations are allowed and whether existing content is preserved.","A text format that preserves nested objects, arrays, numbers, booleans, and text for structured data exchange.","A decorative feature with no effect on the task","A plain-text format for flat rows and columns that is easy to exchange with spreadsheets but stores values as text."],"answer":3,"explanation":"A plain-text format for flat rows and columns that is easy to exchange with spreadsheets but stores values as text."},{"question":"Which explanation best describes JSON?","choices":["A plain-text format for flat rows and columns that is easy to exchange with spreadsheets but stores values as text.","A decorative feature with no effect on the task","A text format that preserves nested objects, arrays, numbers, booleans, and text for structured data exchange.","The instruction passed to open—such as read, write, or append—that determines what operations are allowed and whether existing content is preserved."],"answer":2,"explanation":"A text format that preserves nested objects, arrays, numbers, booleans, and text for structured data exchange."}]
translationStatus: "Draft — native speaker review required"
---

# File Handling & Data Processing

> Read and write files safely, choose CSV for flat tables and JSON for structured data, and use context managers and libraries to avoid data loss.

## Learning objectives

- Explain File mode in your own words.
- Apply CSV to a realistic classroom or community example.
- Complete the practice task and reflect on one improvement.

## Lesson notes

### 1. File mode

The instruction passed to open—such as read, write, or append—that determines what operations are allowed and whether existing content is preserved.

### 2. CSV

A plain-text format for flat rows and columns that is easy to exchange with spreadsheets but stores values as text.

### 3. JSON

A text format that preserves nested objects, arrays, numbers, booleans, and text for structured data exchange.

## Guided practice

Load a small CSV of community resources, calculate a count by category, and save a JSON summary. Handle a missing file without crashing the program.

Work in pairs when devices are shared. Write your prediction before using a device, then compare the result with what actually happened.

## Check your understanding

- What is the most important idea from this lesson?
- Where could you use it at school, at work, or in your community?
- What would you teach to someone seeing this topic for the first time?

## Muhtasari wa Kiswahili — rasimu

Soma na uandike faili kwa usalama, chagua CSV kwa jedwali rahisi na JSON kwa data yenye muundo, na tumia context managers na maktaba kuzuia upotevu wa data.

> Rasimu hii inahitaji mapitio ya mzungumzaji asilia kabla ya kuchapishwa rasmi.

## Résumé français — brouillon

Lisez et écrivez des fichiers en sécurité, choisissez CSV pour les tableaux plats et JSON pour les données structurées, et utilisez les gestionnaires de contexte pour éviter les pertes.

> Cette traduction doit être relue par une personne francophone avant publication officielle.
